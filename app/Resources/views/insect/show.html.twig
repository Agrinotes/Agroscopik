{% extends 'base.html.twig' %}

{% block body %}
    <h1>Insect</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ insect.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ insect.name }}</td>
            </tr>
            <tr>
                <th>Latin</th>
                <td>{{ insect.latin }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('insect_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('insect_edit', { 'id': insect.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
