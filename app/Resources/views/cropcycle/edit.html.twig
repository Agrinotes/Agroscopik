{% extends 'base.html.twig' %}

{% block body %}
    <h1>CropCycle edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Enregistrer" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('cropcycle_index') }}">Back to the list</a>
        </li>
        {% if app.user is_granted('DELETE', cropCycle) or is_granted('ROLE_ADMIN') %}
            <li>
                {{ form_start(delete_form) }}
                    <input type="submit" value="Supprimer">
                {{ form_end(delete_form) }}
            </li>
        {% endif %}
    </ul>
{% endblock %}
