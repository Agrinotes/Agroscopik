{% extends 'base.html.twig' %}

{% block body %}
    <h1>Farm</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ farm.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ farm.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('farm_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('farm_edit', { 'id': farm.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
