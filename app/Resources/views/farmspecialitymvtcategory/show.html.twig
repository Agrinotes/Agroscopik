{% extends 'base.html.twig' %}

{% block body %}
    <h1>FarmSpecialityMvtCategory</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ farmSpecialityMvtCategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ farmSpecialityMvtCategory.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('farmspecialitymvtcategory_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('farmspecialitymvtcategory_edit', { 'id': farmSpecialityMvtCategory.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
