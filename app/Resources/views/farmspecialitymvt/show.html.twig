{% extends 'base.html.twig' %}

{% block body %}
    <h1>FarmSpecialityMvt</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ farmSpecialityMvt.id }}</td>
            </tr>
            <tr>
                <th>Datetime</th>
                <td>{% if farmSpecialityMvt.datetime %}{{ farmSpecialityMvt.datetime|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ farmSpecialityMvt.amount }}</td>
            </tr>
            <tr>
                <th>Unit</th>
                <td>{{ farmSpecialityMvt.unit.name }}</td>
            </tr>
            <tr>
                <th>Priceperunit</th>
                <td>{{ farmSpecialityMvt.pricePerUnit }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('farmspecialitymvt_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('farmspecialitymvt_edit', { 'id': farmSpecialityMvt.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
