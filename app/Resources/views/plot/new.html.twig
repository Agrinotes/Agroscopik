{% extends 'base.html.twig' %}

{% block body %}
    <h1>Plot creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enregistrer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('plot_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
