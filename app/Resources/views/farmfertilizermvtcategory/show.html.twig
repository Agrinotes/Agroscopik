{% extends 'base.html.twig' %}

{% block body %}
    <h1>FarmFertilizerMvtCategory</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ farmFertilizerMvtCategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ farmFertilizerMvtCategory.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ farmFertilizerMvtCategory.slug }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('farmfertilizermvtcategory_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('farmfertilizermvtcategory_edit', { 'id': farmFertilizerMvtCategory.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
