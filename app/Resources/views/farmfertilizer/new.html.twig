
{{ form_start(form, {'action': path('farmfertilizer_new'), 'method': 'POST'}) }}
<div class="row" id="intervention">
    <div class="col-sm-0 col-md-2"></div>
    <div class="col-sm-12 col-md-8">
        <div class="form-group">
            {{ form_label(form.fertilizer) }}
            {{ form_errors(form.fertilizer) }}
            {{ form_widget(form.fertilizer) }}
            <a class="pull-right helpLink" href="{{ path('fertilizer_new') }}">
                Ajouter un engrais ou amendement qui n'est pas dans la liste déroulante
            </a>
        </div>
    </div>
</div>

<div class="row" style="margin-top:20px;">
    <div class="col-sm-0 col-md-2"></div>
    <div class="col-sm-12 col-md-8" style="text-align:right;">
        <button class="btn btn-info margin-left-10" type="submit">Enregistrer</button>
        {{ form_end(form) }}
    </div>
</div>
