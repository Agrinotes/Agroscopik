{% extends 'base.html.twig' %}

{% block body %}
    <h1>FarmFertilizerMvt</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ farmFertilizerMvt.id }}</td>
            </tr>
            <tr>
                <th>Datetime</th>
                <td>{{ farmFertilizerMvt.datetime }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ farmFertilizerMvt.amount }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ farmFertilizerMvt.price }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('farmfertilizermvt_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('farmfertilizermvt_edit', { 'id': farmFertilizerMvt.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
