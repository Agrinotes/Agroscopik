{% extends 'base.html.twig' %}

{% block body %}
    <h1>Catégorie d'interventions</h1>

    <table>
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ interventionCategory.name }}</td>
            </tr>

        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('interventioncategory_index') }}">Liste des catégories d'intervention</a>
        </li>
        <li>
            <a href="{{ path('interventioncategory_edit', { 'id': interventionCategory.id }) }}">Editer</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
