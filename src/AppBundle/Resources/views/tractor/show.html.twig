{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tractor</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tractor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tractor.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tractor_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tractor_edit', { 'id': tractor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
