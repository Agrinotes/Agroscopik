{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter un tracteur</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enregistrer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('tractor_index') }}">Retourner à la liste des outils</a>
        </li>
    </ul>
{% endblock %}
