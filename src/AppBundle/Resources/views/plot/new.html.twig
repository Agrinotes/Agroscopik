{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nouvelle parcelle</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enregistrer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('farm_show_current') }}">Annuler</a>
        </li>
    </ul>
{% endblock %}
