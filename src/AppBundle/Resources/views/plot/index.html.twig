
    <h1>Liste des parcelles</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for plot in plots %}
            {% if is_granted('VIEW', plot) or is_granted('ROLE_ADMIN') %}
            <tr>
                <td><a href="{{ path('plot_show', { 'id': plot.id }) }}">{{ plot.id }}</a></td>
                <td>{{ plot.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('plot_show', { 'id': plot.id }) }}">Voir</a>
                        </li>
                        <li>
                            <a href="{{ path('plot_edit', { 'id': plot.id }) }}">Editer</a>
                        </li>
                    </ul>
                </td>
            </tr>
            {% endif %}
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('plot_new') }}">Ajouter une parcelle</a>
        </li>
    </ul>

