{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter une culture</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enregistrer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('crop_index') }}">Retourner à la liste des cultures</a>
        </li>
    </ul>
{% endblock %}
