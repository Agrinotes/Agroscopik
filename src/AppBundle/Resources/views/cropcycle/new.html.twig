{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajouter un cycle de culture sur {{ plot.name }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enregistrer" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('plot_show',{'id': plot.id }) }}">Retourner à la parcelle</a>
        </li>
    </ul>
{% endblock %}
